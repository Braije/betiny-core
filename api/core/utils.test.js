
const $ = require("../index.js");
const assert = require('assert');

describe('Utils', () => {

    it('delay', () => {
        assert.equal(false, false);
    });

    it('replace', () => {
    });

});
